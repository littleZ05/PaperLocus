# Sample Output

This is a shortened smoke-test output for a title-only request. It demonstrates
the intended caution level when PaperLocus has not inspected the full paper.

```markdown
# Attention Is All You Need

## 一句话总结
仅从标题推断，这篇论文大概率主张：在序列建模里，`attention` 可以从辅助模块上升为核心架构；但由于这里没有读取论文正文，这个判断只能算低置信度预判。

## 论文卡片
- 标题：`Attention Is All You Need`
- 输入条件：仅有标题，未读取摘要、正文、图表或参考文献
- 笔记级别：title-only smoke test

## 论文类型
- 推断：更像计算机科学方法论文，而不是以科学发现为主的证据链论文。
- 不确定性：仅凭标题无法排除它也可能是一篇较强观点表达的系统论文。

## 文献位置
- 推断：标题语气很强，像是在反驳“序列模型必须依赖其他核心机制”的既有默认设定，并把 `attention` 推到主干位置。
- 不确定性：这里还不知道它具体在对比 RNN、CNN、还是更早的 encoder-decoder 变体。

## 引言主线
- 推断：论文可能会先指出现有序列模型在建模长程依赖、并行训练或结构复杂度上的问题，再提出“只靠 attention 就够”的方法主张。
- 不确定性：这是基于领域记忆的常见叙事模板，不是当前输入直接提供的事实。

## 方法框架
- 论文主张：当前无法从标题直接确认具体方法。
- 推断：大概率会提出一个以 `attention` 为核心的完整模型，而不是只加一个新模块。
- 不确定性：不知道是否包含编码器-解码器、位置编码、多头机制等具体设计。

## 实验与结果
- 论文主张：当前未知。
- 推断：如果它是典型方法论文，正文通常会用基准任务对比来证明纯 `attention` 架构在效果或效率上的优势。
- 不确定性：任务、数据集、指标、基线、结论强度都不能从标题推出。

## 主要贡献
- 推断：核心贡献可能是把 `attention` 从配角改成主角，并据此重写序列建模范式。
- 不确定性：这只是对标题修辞的解释，不等于论文已被验证的精确贡献列表。

## 局限、反例与检查点
- 只看标题时，最容易高估的是“它究竟说的是研究结论，还是宣传式命名”。
- 下一步最值得补的是摘要；只要看到摘要，就能把“论文主张”和“我的推断”分得更干净。

## 值得细读的部分
- 摘要：确认真正 claim
- 引言：确认它在反驳谁、替代谁
- 方法部分：确认 `attention` 是否真是唯一主干
- 实验部分：确认结论是效果优势、效率优势，还是两者都有
```
